const works = ['module unclock at 8pm', 'practice after watch the video', 'collect note to watch video', 'own practice after finishing module', 'joined support session if do not understand any topics'];
/* let i = 1;
while (i <= 10) {
    console.log(works);
    i++;
} */
for (let i = 1; i <= 10; i++) {
    console.log(works);
}